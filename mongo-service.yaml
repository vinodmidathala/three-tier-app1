apiVersion: v1
kind: Service
metadata:
  namespace: noteapp
  name: mongodb-svc
spec:
  tyep: NodePort
  selector:
    app: mongodb
  ports:
  - name: mongodb-svc
    protocol: TCP
    port: 27017
    targetPort: 27017